\begin{center}
{\smaller
    \begin{tabular}{ l{% for c in head %} {% if not loop.first %} r {% endif %} {% endfor %} }
    \toprule
    {% for col in head %}
        \textbf{ {{col|safe}} }
        {% if not forloop.last %}
           &
        {% endif %}
    {% endfor %}
    \\
    \midrule
    {% for row in table %}
        {% for cell in row %}
            {{cell}}
            {% if not forloop.last %}
               &
            {% endif %}
        {% endfor %}
        \\
    {% endfor %}
    \bottomrule
    \end{tabular}
}
\end{center}
